# ===========================================
# Docker Environment Variables
# Mesa Virtual
# ===========================================
# Copia este archivo a .env y ajusta los valores

# ===========================================
# MongoDB Configuration
# ===========================================
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=tu_password_seguro_mongodb

# ===========================================
# MinIO Configuration (Storage)
# ===========================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=tu_password_seguro_minio

# ===========================================
# JWT Secrets (IMPORTANTE: cambiar en produccion)
# ===========================================
SECRET=tu_jwt_secret_muy_seguro_y_largo
REFRESH_SECRET=tu_refresh_secret_muy_seguro_y_largo

# ===========================================
# Public URL
# Esta es la URL publica donde estara expuesto el servicio
# Si usas Cloudflare Tunnel, seria algo como: https://tudominio.com
# ===========================================
PUBLIC_URL=https://tudominio.com

# ===========================================
# SMTP Configuration (Email)
# ===========================================
SMTP_HOSTNAME=smtp.mailgun.org
SMTP_PORT=587
SMTP_USERNAME=tu_usuario_smtp
SMTP_PASSWORD=tu_password_smtp

# ===========================================
# Cloudflare Tunnel
# ===========================================
# Para obtener el token:
# 1. Ve a https://one.dash.cloudflare.com/
# 2. Accede a Networks > Tunnels
# 3. Crea un nuevo tunnel o selecciona uno existente
# 4. En la configuracion del tunnel, copia el token
# 5. Configura el tunnel para apuntar a: http://nginx:80
CLOUDFLARE_TUNNEL_TOKEN=tu_token_de_cloudflare_tunnel
